MAXIFY Frontend B (Weeks 1–3)

Install:
npm install
npx expo install expo-location react-native-maps

Run:
npx expo start

This module provides:
- GPS tracking
- velocity calculation
- activity detection
- context provider for easy integration with Frontend A and backend APIs